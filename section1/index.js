/*
    npm - node package manager
    npm init - npm 초기화
    node 파일명 => node로 js 파일을 실행 (package.json과 같은 경로일때)
    다른 경로 일 때에는 경로까지 정확히 명시 해주어야 오류가 안난다.
    packagescript - package.json 안에 있는 scripts안에 설정할 수 있는 일종의 매크로 형식
    (npm run 매크로로 정의한 명령어)
*/
console.log("Hello Node.js Start")

